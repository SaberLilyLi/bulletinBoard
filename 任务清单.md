# 智绘任务台（SmartTask）需求文档

## 一、项目概述

**定位**：企业级团队协作工具，专注任务管理+实时协作+数据可视化  
**核心价值**：  
✅ 极致性能（500+任务流畅拖拽）  
✅ TS深度实践（100%类型安全）  
✅ 组件可复用（封装率>70%）

---

## 二、任务看板模块详细需求

### 1. 功能清单

| 功能             | 优先级 | 交互说明                                                 |
| ---------------- | ------ | -------------------------------------------------------- |
| **三列状态看板** | P0     | 待办/进行中/已完成 3列，状态标签颜色区分（蓝/黄/绿）     |
| **任务卡片**     | P0     | 标题+状态标签+负责人头像+创建时间+优先级标记（高/中/低） |
| **拖拽排序**     | P0     | 任务卡片跨列拖拽，拖拽时显示目标列高亮提示               |
| **WIP限制**      | P1     | 每列最多5个任务，超限时禁止拖拽（提示"已达上限"）        |
| **任务创建**     | P0     | 点击"新建任务"弹出表单（标题/优先级/负责人）             |
| **任务编辑**     | P1     | 双击卡片标题进入编辑模式（修改标题/优先级）              |

### 2. TS类型设计

```ts
// src/types/TaskType.ts
export type TaskStatus = 'todo' | 'doing' | 'done'
export type TaskPriority = 'high' | 'medium' | 'low'

export interface Task {
  id: string
  title: string
  status: TaskStatus
  priority: TaskPriority
  assignee: { name: string; avatar: string }
  createdAt: Date
  updatedAt: Date
}

// 看板列定义
export interface Column {
  id: 'todo' | 'doing' | 'done'
  title: string
  tasks: Task[]
  wipLimit?: number // WIP限制（默认5）
}
```
